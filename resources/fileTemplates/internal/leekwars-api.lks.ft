// This file was automatically generated by IntelleekJ, it is not intended to be edited.

#foreach($weapon in $weapons)
/**
 * ${weapon.name}. ${weapon.description}
 */
global ${weapon.id};

#end
#foreach($chip in $chips)
/**
 * ${chip.name}. ${chip.description}
 */
global ${chip.id};

#end